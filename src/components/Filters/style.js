import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  filterArea: {
    marginHorizontal: 5,
    alignItems: 'center'
  },
  text: {
    color: '#FFF',
    fontSize: 15,
    marginTop: 5
  }
})

export default style